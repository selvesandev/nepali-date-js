# Publish

1) update the version name on `package.json` file 
2) `npm login` and `npm whoami`
3) `npm run build` 
4) `npm pack --dry-run`  to check what will be published
5) `npm publish` to publish the package